<%= to_button(
  bands_url, :post, "Create".to_russian, children: [
    to_input(type: "text", name: "band[name]", placeholder:"Enter Band Name"),
    to_input(type: "text", name: "band[image_url]", placeholder:"Enter Band Image URL")
  ]
) %>